paths:
  raw_dir: "data/raw"
  out_dir: "data/out"
  index_dir: "data/index"

ocr:
  engine: "paddleocr"
  lang: "en"
  min_conf: 0.0

pdf:
  zoom: 2.5

chunking:
  max_chars: 1100
  min_chars: 1

retrieval:
  top_k: 8

bedrock:
  model_id: ""

extraction:
  output_format: "both"

debug:
  enabled: true
